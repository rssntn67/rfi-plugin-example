<blueprint xmlns="http://www.osgi.org/xmlns/blueprint/v1.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
           xmlns:cm="http://aries.apache.org/blueprint/xmlns/blueprint-cm/v1.1.0"
           xsi:schemaLocation="http://www.osgi.org/xmlns/blueprint/v1.0.0
                http://www.osgi.org/xmlns/blueprint/v1.0.0/blueprint.xsd
                http://aries.apache.org/blueprint/xmlns/blueprint-cm/v1.1.0
                http://aries.apache.org/schemas/blueprint-cm/blueprint-cm-1.1.0.xsd">

    <!-- Configuration stored in $OPENNMS_HOME/etc/it.xeniaprogetti.rfi.plugin.example.cfg file -->
    <cm:property-placeholder id="rfi-plugin-examplePluginProperties" persistent-id="it.xeniaprogetti.rfi.plugin.example" update-strategy="reload">
        <cm:default-properties>
            <cm:property name="url" value="http://127.0.0.1:8888/target" />
            <cm:property name="apiKey" value="TOKEN" />
        </cm:default-properties>
    </cm:property-placeholder>

    <!-- Pull in service implementations from the OSGi registry -->
    <reference id="nodeDao" interface="org.opennms.integration.api.v1.dao.NodeDao"/>
    <reference id="alarmDao" interface="org.opennms.integration.api.v1.dao.AlarmDao"/>
    <reference id="edgeDao" interface="org.opennms.integration.api.v1.dao.EdgeDao"/>
    <reference id="eventForwarder" interface="org.opennms.integration.api.v1.events.EventForwarder"/>
    <reference id="eventSubscriptionService" interface="org.opennms.integration.api.v1.events.EventSubscriptionService"/>

    <!-- Build our API client -->
    <bean id="apiClient" class="it.xeniaprogetti.rfi.plugin.example.clients.ApiClient">
        <argument value="${url}"/>
        <argument value="${apiKey}"/>
    </bean>

    <bean id="connectionManager" class="it.xeniaprogetti.rfi.plugin.example.connection.ConnectionManager">
        <argument>
            <reference interface="org.opennms.integration.api.v1.runtime.RuntimeInfo" availability="optional"/>
        </argument>
        <argument>
            <reference interface="org.opennms.integration.api.v1.scv.SecureCredentialsVault" availability="optional"/>
        </argument>
    </bean>
    <service ref="connectionManager" interface="it.xeniaprogetti.rfi.plugin.example.connection.ConnectionManager"/>

    <bean id="snmpClient" class="it.xeniaprogetti.rfi.plugin.example.clients.SnmpClient"/>
    <service ref="snmpClient" interface="it.xeniaprogetti.rfi.plugin.example.clients.SnmpClient"/>

    <bean id="syncService" class="it.xeniaprogetti.rfi.plugin.example.SyncService">
        <argument ref="snmpClient"/>
        <argument ref="connectionManager"/>
    </bean>
    <service ref="syncService" interface="it.xeniaprogetti.rfi.plugin.example.SyncService"/>

    <!-- Build our forwarder bean -->
    <bean id="alarmForwarder" class="it.xeniaprogetti.rfi.plugin.example.AlarmForwarder">
        <argument ref="apiClient"/>
        <argument ref="eventForwarder"/>
    </bean>
    <!-- Expose the forwarder as a AlarmLifecycleListener so we get callbacks -->
    <service interface="org.opennms.integration.api.v1.alarms.AlarmLifecycleListener" ref="alarmForwarder"/>
    <!-- Expose the forwarder directly for access from our shell commands -->
    <service interface="it.xeniaprogetti.rfi.plugin.example.AlarmForwarder" ref="alarmForwarder"/>

    <!-- Build our forwarder bean -->
    <bean id="topologyForwarder" class="it.xeniaprogetti.rfi.plugin.example.TopologyForwarder">
        <argument ref="apiClient"/>
        <argument ref="edgeDao"/>
    </bean>
    <!-- Expose the forwarder directly for access from our shell commands -->
    <service interface="it.xeniaprogetti.rfi.plugin.example.TopologyForwarder" ref="topologyForwarder"/>

    <!-- Build and expose the JAX-RS annotated webhook handler.
         This will automatically mount the endpoint over the REST API. -->
    <bean id="webhookHandlerImpl" class="it.xeniaprogetti.rfi.plugin.example.webhook.WebhookHandlerImpl" >
        <argument ref="syncService"/>
        <argument ref="connectionManager"/>
    </bean>
    <service interface="it.xeniaprogetti.rfi.plugin.example.webhook.WebhookHandler" ref="webhookHandlerImpl">
        <service-properties>
            <entry key="application-path" value="/rest" />
        </service-properties>
    </service>

    <!-- Expose our custom event definitions bundled in the .jar -->
    <service interface="org.opennms.integration.api.v1.config.events.EventConfExtension">
        <bean class="it.xeniaprogetti.rfi.plugin.example.events.EventConfExtension"/>
    </service>

    <!--bean id="aComEventIngestor" class="it.xeniaprogetti.rfi.plugin.example.events.AComEventIngestor"
          init-method="start" destroy-method="destroy">
        <argument ref="eventForwarder"/>
        <argument ref="nodeDao"/>
        <argument ref="alarmDao"/>
        <argument ref="eventSubscriptionService"/>
    </bean-->

</blueprint>